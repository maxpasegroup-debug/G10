PORT=5000
NODE_ENV=development
DATABASE_URL=postgresql://USER:PASSWORD@YOUR_DB_HOST:5432/music_academy
JWT_SECRET=supersecretkey

# CORS: comma-separated allowed origins. In production (NODE_ENV=production), * is rejected — set your frontend URL(s), e.g. https://app.example.com,https://www.example.com
CORS_ORIGIN=*

# Optional rate limits (per IP, per window)
# RATE_LIMIT_MAX_PER_WINDOW=300
# AUTH_RATE_LIMIT_MAX_PER_WINDOW=30
